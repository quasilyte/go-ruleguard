# This file adheres to the YAML5 style.
{
  name: "Merge",
  on: {
    push: {
      branches: ["master"],
    }
  },
  jobs: {
    build: {
      name: "Build",
      "runs-on": "ubuntu-latest",
      steps: [
        {name: "Test master", run: "make test-master"},
      ],
    },
  },
}
